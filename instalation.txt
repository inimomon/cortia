# CORTIA Anomaly Detection System - Python Dependencies
# Version: 1.0.0
# Date: April 2026
# Python Version: 3.9+

# Installation: pip install -r installation.txt
# Virtual Environment: python -m venv venv && source venv/bin/activate

# Core Machine Learning & Data Processing
scikit-learn==1.3.2              # Isolation Forest anomaly detection
shap==0.43.0                     # SHAP TreeExplainer for explainability
pandas==2.0.3                    # Data manipulation & analysis
numpy==1.24.0                    # Numerical computations
scipy==1.11.4                    # Scientific computing

# Serialization & Model Persistence
joblib==1.3.0                    # Model serialization (for .joblib artifacts)
skl2onnx=1.20.0                  # Convert scikit-learn models to ONNX format
onnxruntime=1.24.4               # ONNX model inference engine

# Web Framework & API
fastapi==0.104.0                 # REST API framework
uvicorn==0.24.0                  # ASGI web server
pydantic==2.5.0                  # Data validation

# Database
mysql-connector-python==8.0.33   # MySQL connector for historical predictions
SQLAlchemy==2.0.23               # ORM (optional, for advanced queries)

# Data Visualization & Dashboard
matplotlib==3.7.1                # Static plots

# Development & Testing
pytest==7.4.3                    # Unit testing framework
pytest-cov==4.1.0                # Code coverage for tests
flake8==6.1.0                    # Code linting (optional)
black==23.11.0                   # Code formatting (optional)

# Jupyter Environment (for notebooks)
jupyter==1.0.0                   # Jupyter notebook support
notebook==7.0.6                  # Classic Notebook interface
ipython==8.17.2                  # Interactive Python shell

# Utilities
python-dotenv==1.0.0             # Environment variable management
tqdm==4.66.1                     # Progress bars
requests==2.31.0                 # HTTP requests (for API calls)

# CRITICAL NOTES:
# 1. scikit-learn 1.3.2: Compatible with Isolation Forest & SHAP TreeExplainer
#    - Do NOT upgrade to 1.5+ without testing model compatibility
#    - Previous joblib artifacts may fail with major version mismatches

# 2. shap 0.43.0: Stable version for TreeExplainer
#    - Ensure consistency between training & inference environments

# 3. fastapi & uvicorn: Production-ready versions
#    - Use uvicorn --host 0.0.0.0 --port 8000 for server startup

# 4. mysql-connector-python: Version 8.0.33 recommended for MySQL 5.7+
#    - Alternative: PyMySQL==1.1.0 if conn pooling needed

# 5. For deployment containers:
#    - Docker: Use python:3.9-slim as base image
#    - Ensure TLS/SSL certificates installed for MySQL secure connections

# INSTALLATION VERIFICATION:
# After installation, verify with:

#   python -c "import sklearn, shap, fastapi; print('Core deps OK')"
#   python -c "import joblib; model = joblib.load('path/to/master_model.joblib'); print('Model loads OK')"